let express,models;_134‍.x([["default",()=>_134‍.o]]);_134‍.w("express",[["default",["express"],function(v){express=v}]]);_134‍.w("../models",[["default",["models"],function(v){models=v}]]);


const router = express.Router()
const getHandler = (req, res) => {
    const query = JSON.stringify(req.query);
    res.send("testing get " + query)
}
const postHandler = (req, res) => {
    const body = req.body;
    const user = new models.User({ username: body.username, createAt: new Date() });
    user.save().then((savedUser) => {
        res.status(201).send('User saved.Id' + savedUser._id);
    }).catch((error) => {
        console.log("error");
        res.status(500).send(error);
    });
};
router.get('/', getHandler);
app.post('/', postHandler);

const configure = (app) =>{
    app.use('/users',router);
}
_134‍.d(configure);