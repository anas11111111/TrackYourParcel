let express,models,saveUser,getAllUsers,update,deleteById;_9e0‍.x([["default",()=>_9e0‍.o]]);_9e0‍.w("express",[["default",["express"],function(v){express=v}]]);_9e0‍.w("../models",[["default",["models"],function(v){models=v}]]);_9e0‍.w("../services/userService",[["saveUser",["saveUser"],function(v){saveUser=v}],["getAllUsers",["getAllUsers"],function(v){getAllUsers=v}],["update",["update"],function(v){update=v}],["deleteById",["deleteById"],function(v){deleteById=v}]]);



const router = express.Router()
const getHandler = async (req, res) => {
    // const query = JSON.stringify(req.query);
    // res.send("testing get " + query)
    const users = await getAllUsers();
    res.status(200).send(users);
}
const postHandler = async (req, res) => {
    const body = req.body;
    const user = await saveUser(body);
    res.status(201).send(user._id);

};
const putHandler = async (req, res) => {
    const body = req.body;
    const user = await update(body);
    res.status(200).send(user._id);
}

const deleteHandler = async (req, res) => {
    const id = req.params.id;
    _9e0‍.g.console.log(id);

    const result = await deleteById(id);
    if (result instanceof Error) {
        res.status(404).send(result.message);
    }
    else {
        res.status(200).send("User deleted");

    }
}

router.get('/', getHandler);
router.post('/', postHandler);
router.put('/', putHandler);
router.delete('/:id', deleteHandler);

const configure = (app) => {
    app.use('/users', router);
}
_9e0‍.d(configure);